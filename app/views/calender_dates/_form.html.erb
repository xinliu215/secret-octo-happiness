<%= form_for(@calender_date) do |f| %>
  <% if @calender_date.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@calender_date.errors.count, "error") %> prohibited this calender_date from being saved:</h2>

      <ul>
      <% @calender_date.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
